import { MenuItem } from 'electron';
export declare class Menubar {
    static createSubmenu(element: Element, items: Array<MenuItem>, isSubmenu: boolean): void;
    static setEvents(hoverColor: string): void;
    static getLabelFormat(label: string): HTMLElement;
    static cleanMnemonic(label: string): string;
}
